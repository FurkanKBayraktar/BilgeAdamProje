
@{
    ViewBag.Title = "List";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}
@using PagedList;
@using PagedList.Mvc;
@model IPagedList<BilgeAdam.Model.Model.Entities.Store>


<div class="container-fluid">
    <a href="@Url.Action("Add","Store",new { area="Member"})"><div class="btn btn-bitbucket"><i class="ion ion-person-add"></i> Yeni Şube Ekle </div></a>
    <div class="box panel-body">
        <div class="col-lg-12">
            <h3 class="text-teal">Şube Listesi</h3>
            <hr />
            <table class="table table-hover table-bordered table-striped">
                <thead>
                    <tr>
                        <th>Şube Adı</th>
                        <th>Adres</th>
                        <th>Telefon</th>
                        <th>Email</th>
                        <th class="col-lg-2">İşlem</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.Name</td>
                            <td>@item.Address</td>
                            <td>@item.Phone</td>
                            <td>@item.Email</td>
                            <th>
                                <div class="btn btn-justified btn-block text-nowrap">
                                    <a href="/Member/Role/Update/@item.ID" class="btn btn-success"><i class="fa fa-pencil-square-o"></i> Güncelle </a>
                                    <a href="/Member/Role/Delete/@item.ID" class="btn btn-danger"><i class="fa fa-times"></i> Sil </a>
                                </div>
                            </th>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
        <div class="pagedList">
            @Html.PagedListPager(Model, page => Url.Action("List", "Store", new { page }), PagedListRenderOptions.TwitterBootstrapPager)
        </div>
    </div>
</div>

